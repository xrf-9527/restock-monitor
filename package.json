{
  "name": "restock-monitor",
  "version": "1.0.0",
  "description": "Cloudflare Worker for monitoring BandwagonHost and DMIT restock",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.0.4",
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.15",
    "wrangler": "^4.54.0"
  }
}
